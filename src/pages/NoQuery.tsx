import React from "react";
import styles from "../css/Base.module.scss";

const NoQuery = () => {
  return (
    <div className={styles.center}>
      <h3>Search for a movie, person...</h3>;
    </div>
  );
};

export default NoQuery;
